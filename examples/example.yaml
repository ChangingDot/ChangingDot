goal: We want to simulate a private attribute changing name - change Length to Size

analyzer_options:
  solution_path: ./CustomChanges1.sln

initial_change:
  error: We want to simulate a private attribute changing name - change Length to Size
  file_path: ./Level1/Item.cs
  error_position: [8, 0, 8, 0]

commit:
  branch_name: changing_branch
  git_path: ./
  repo_url: git@github.com:ChangingDot/ChangingLevels.git

llm_provider: MISTRAL
